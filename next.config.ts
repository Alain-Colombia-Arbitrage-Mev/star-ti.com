import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // Cloudflare Pages configuration
  // No output mode needed - @cloudflare/next-on-pages handles it
};

export default nextConfig;
